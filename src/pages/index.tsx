// Use node 16.9.0
// URL to fetch "https://jsonplaceholder.typicode.com/todos"
interface Todo {
  id: number;
  title: string;
  completed: boolean;
  userId: number;
}

export default function Home() {
  return <div>Yield Studio</div>;
}
